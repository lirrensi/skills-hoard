# artify-cli

Standalone CLI for the `artify` skill. Self-contained — run it with `uv run`
against this folder and `uv` resolves the declared requirements into a
throwaway environment on the fly. No global install required.

```bash
uv run --project artify/cli artify --help
```
